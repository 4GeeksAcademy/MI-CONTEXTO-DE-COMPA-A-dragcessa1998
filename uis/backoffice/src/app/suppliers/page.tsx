import SuppliersView from "@/components/SuppliersView";

/** Página del Directorio de Proveedores (datos en vivo de la Supplier API — FastAPI). */
export default function SuppliersPage() {
  return <SuppliersView />;
}
